import { Label } from 'radix-ui';
import { CardWrap } from './styled';

const Card = () => {
  return (
    <CardWrap>
      {/* <img src={`${thumbnail.path}/portrait_xlarge.jpg`} alt={name} /> */}
      <Label.Root>Teste</Label.Root>
    </CardWrap>
  );
};

export default Card;
